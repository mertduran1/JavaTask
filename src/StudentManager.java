public class StudentManager {
    public void addDepartment(Student student) {
        System.out.println(student.getFirstName() + " " + student.getLastName() + " Added to " + student.getDepartment());
    }
}
